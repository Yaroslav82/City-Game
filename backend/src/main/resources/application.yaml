spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cities_game_db
    username: dev
    password: abcd1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
